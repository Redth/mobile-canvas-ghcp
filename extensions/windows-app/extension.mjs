// Plugin extension paths are containers. The loader discovers immediate child
// directories, so this entrypoint bridges the plugin layout to the shared
// source-install entrypoint at the repository root.
import "../../windows-extension.mjs";
